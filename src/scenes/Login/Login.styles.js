
import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    container: {
        justifyContent: 'center',
        alignItems: 'center'
    },
    logo: {
        flex: 1,
        height: '100%',
        width: '100%',
        resizeMode: 'cover',
        position: 'absolute'
    }
});